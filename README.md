# Dodo embeddable checkout

One React + TypeScript + Vite app. The merchant demo, checkout, and SDK will live under `src/`.

## Scripts

- `npm run dev` starts the app
- `npm run build` type-checks and builds into `dist`
